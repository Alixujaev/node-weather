const getArgs = require('./helpers/args')

const startCLI = () => {
  const args = getArgs(process.argv);
  console.log(args);

  if(args.h){

  }

  if(args.s){

  }

  if(args.t){
    
  }
}

startCLI()